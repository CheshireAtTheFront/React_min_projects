import "./style.css";

const InputText = () => {
  return (
    <input
      type="text"
      placeholder="Куда было потрачено"
      className="form__text"
      id="value-text"
    />
  );
};

export default InputText;
